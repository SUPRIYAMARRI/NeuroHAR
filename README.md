# NeuroHAR
NeuroHar is a health prediction web application that analyzes user health data and provides predictions using dataset-driven logic. The system allows users to register, log in, upload datasets, and view prediction results through a simple web interface. 
